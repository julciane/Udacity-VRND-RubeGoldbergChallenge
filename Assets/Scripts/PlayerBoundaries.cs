using UnityEngine;
using System.Collections;

public class PlayerBoundaries : MonoBehaviour
{
    private void OnTriggerExit(Collider other)
    {
        //if player is using the ball disable ball because player is cheating
    }
}
